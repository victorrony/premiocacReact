/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    //Erase picsum.photos later
    domains: ["premiocac.com",'picsum.photos'],
  },

}

module.exports = nextConfig
